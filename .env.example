# Copy this file to .env and fill in your own values.
# If OPENAI_API_KEY is not set, the app falls back to a local
# HuggingFace embedding model + open-source LLM automatically.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
